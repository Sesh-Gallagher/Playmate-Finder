/**
 * Configures PostCSS plugins for the project.
 * 
 * The `tailwindcss` plugin is used to apply Tailwind CSS utility classes to the project.
 * The `autoprefixer` plugin is used to automatically add vendor prefixes to CSS properties.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
